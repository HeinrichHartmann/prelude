(add-to-list 'load-path (expand-file-name "~/git/mu/mu4e"))

(require 'mu4e)

(setq mu4e-maildir "~/Mail/Circonus")

(setq mu4e-drafts-folder "/[Gmail].Drafts")
(setq mu4e-sent-folder   "/[Gmail].Sent Mail")
(setq mu4e-trash-folder  "/[Gmail].Trash")


